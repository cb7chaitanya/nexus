// Intentionally empty. This package is where cross-app Zod schemas and
// shared TS types (api/web/worker request-response contracts) will live
// once there's a first route to define a contract for — see
// docs/implementation-plan.md ticket sequencing. Foundation layer only.

export {};
